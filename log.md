# fxadmin Implementation Log

## 2026-08-25 (cont'd) - Dependency update, dev-server cleanup, standing build-verification rule

- Sunny asked to kill the process squatting on port 3000 (blocking his own `yarn dev`) and run `yarn install` to align with the already-pinned `next: 16.1.6` in `package.json` (no longer canary). Killed a leftover background dev-server process of mine that had been holding port 3000 this whole session (should have been stopped earlier — noted for next time). `yarn install` initially failed with `EPERM` on the native SWC binary because Sunny's own `yarn dev` process tree had it locked; stopped that tree (all three processes belonged to the same `next dev` run) to let the install complete, then told him to restart `yarn dev` himself.
- Clean `yarn build` afterward (fresh `.next`, no stale cache): 35/35 routes, no errors, confirmed on Next.js 16.1.6 (Turbopack).
- Sunny set a standing rule: always run `yarn build` after tests/checks, not just lint/typecheck — saved to this project's Claude memory (`feedback_always_build.md`) so it persists across sessions.

## 2026-08-25 (cont'd) - Premium redesign (mockup → code) and FX-engine API-key redundancy fixed

- **API-key bug fixed**: `proxy.ts`'s matcher never covered `/api/fx/rate`/`/api/fx/history`, so `RateEngine.tsx` (the dashboard's FX Rate Engine tab) separately demanded a static API key even for an already-logged-in session. Added `verifyApiKeyOrSession()` (`lib/fx-engine/utils/auth.ts`) so both routes accept either the existing external `x-api-key` or a live Supabase session; removed the manual key `<input>`/"Load" gate from `RateEngine.tsx` entirely (auto-fetches on mount now). Also fixed a second, independent bug in the same component: "Refresh Rate" was sending the pasted key to `/api/cron/update-rates`, which actually checks `CRON_SECRET` — a different secret — so it never worked; since `calculateFinalFxRate()` already persists on every `/api/fx/rate` call, "Refresh" now just re-fetches instead of calling the cron endpoint.
- **Sunny called the first rebrand "horrible"**: a full audit (`primary`/`accent` were the literal same hex, a translucent cyan wash was baked into every Card/Input border, no neutral scale existed, 4 competing "hero glow" cards, unused chart tokens, no typeface actually loading, a `dropbg-primary` bug painting the whole select dropdown solid cyan) — findings and full root-cause detail in this session's plan file.
- Per Sunny's direction, mocked the redesign first as a 4-artboard `/design` canvas (Dashboard/FX-Rate-Engine, a data table, Calculator, Rate Management) before touching code — published as a Claude Artifact, reviewed, fixed two real issues a background check caught (hero number using the wrong token, a legend/line dash mismatch).
- **Sunny then gave an exact, different palette mapping** than the initial all-dark mockup: sidebar `#001619` bg / `#c7f8fe` text, main content `#c7f8fe` bg / `#001619` text, `#50e8f4` as the one accent throughout. Reworked all 4 mockup artboards to that spec, republished to the same artifact link, then said "proceed."
- **Ported into the real app**: rewrote `app/globals.css`'s token block so `--color-bg`/`--color-foreground` (main canvas, used everywhere via `bg-background`/`text-foreground` including login/signup) flip to light while `--color-sidebar`/`--color-sidebar-foreground` stay dark — the app's existing token architecture already separated these, so this one edit propagated correctly to every page with zero hardcoded colors (confirmed by spot-checking `binance/page.tsx`, identical pattern to 5 other table pages). Removed the dark radial-gradient `body` background (main canvas is flat light now). Status colors (`success`/`danger`) got darker shades (`#16a34a`/`#dc2626`) for legibility as bare text on the light canvas.
- **Follow-on component fixes surfaced by the flip**: `Sidebar.tsx` had a hardcoded gradient instead of `bg-sidebar`, and both it and the mobile header (`app/dashboard/layout.tsx`) were relying on the *global* (now-light) text/border tokens for text rendered on the *dark* sidebar bg — invisible/wrong without a fix, switched to `text-sidebar-foreground`/`border-sidebar-border`. `RateChart.tsx`'s baseline line was hardcoded `#c7f8fe`, which is now literally the same as the page background (invisible) — fixed to reference `var(--chart-neutral)`, and wired `--chart-usd-fiat` for the primary line too (both tokens existed but were unused before). Fixed the `dropbg-primary` bug in `components/ui/select.tsx` and softened `button.tsx`'s `outline`/`ghost` hover states (were a full solid-cyan fill; now a soft `/10` tint) to match the "accent reserved for real emphasis" system. Fixed the calculator page's "Last" badge, which was reusing the brand accent as a fake status color next to a correctly-green "Live" badge — now neutral. Deleted `components/Topbar.tsx` (confirmed genuinely dead — only a commented-out import remained) and cleaned the ~50-line dead commented-out old layout block in `app/layout.tsx`.
- Verified: `yarn build` clean (35/35 routes, one genuine TS error caught and fixed along the way — a cookie-handler callback needed explicit typing). Not yet done: on-screen visual QA in a real browser, desktop + mobile.
- **`/21st-design-sync` explicitly declined**: asked whether to publish the finished token system as a public 21st.dev theme (the plan's last step); Sunny said not to, keep it private. Skipped — no public/external publish happened.

## 2026-08-25 - Full audit, rebrand, security/bug fixes; public rate API blocked on prod DB check

- **Audit**: three-agent exploration (repo/bugs, theming, FX rate engine) plus direct file reads surfaced critical security holes, dead/broken routes, and a single-source color system. Full findings and the approved plan are in the plan file used for this session; key items below.
- **Blocking discovery (Workstream 0, unresolved)**: queried the production Supabase project (`ompwoeanfczefuxndppa`, confirmed by Sunny to be what production actually uses) directly — `fx_rate_calculations` and `otc_desk_rates` have no rows newer than 2025-12-13, 8+ months stale, despite Sunny setting ₦1435.00 via `/dashboard/management` ~30 minutes before this session. Root cause not yet confirmed: either `RateManagement.tsx`'s OTC "Implied Rate" preview (pure client-side `ngn_cost/usd_cost` math, never persisted, not the real engine formula) was mistaken for the actual engine output, or the real write-back in `calculateFinalFxRate()` is silently failing in production. **The new public partner rate API (Workstream 4) is not built yet — intentionally blocked until this is resolved**, since it was going to read from `latest_fx_rate` and that view is not currently trustworthy.
- **Theme rebrand** to `#001619` (bg) / `#50e8f4` (primary/accent) / `#c7f8fe` (foreground): rewrote `app/globals.css`'s CSS variable palette (single unified theme now — deleted the near-duplicate `@media (prefers-color-scheme: dark)` block that was mislabeled "LIGHT MODE" but wasn't a real light theme), plus every hardcoded old-brand hex found in `Sidebar.tsx`, `Topbar.tsx`, `PriceChart.tsx`, `RateChart.tsx`, `app/dashboard/page.tsx`, `app/dashboard/calculator/page.tsx`, `app/icon.svg`, and the dead commented-out block in `tailwind.config.ts`.
- **Security fixes**:
  - Removed the admin API-key display/copy UI from `RateManagement.tsx` (was reading `NEXT_PUBLIC_INTERNAL_API_KEYS`, shipping the real keys in the client JS bundle).
  - Added `middleware.ts` + cookie-backed Supabase client (`lib/supabase/browser.ts`, `lib/supabase/server.ts`, `lib/supabase/middleware.ts` — migrated `lib/auth-context.tsx` off the old localStorage-only client) to gate `/dashboard/*` and `/api/fx/internal/*` server-side, replacing the previous client-only `useEffect` redirect.
  - `otc-desk` and `crypto-rates` internal routes now check a real Supabase session instead of the leaked static key, and write via a new service-role client (`lib/supabaseAdmin.ts`) instead of the anon client.
  - Folded `RateManagement.tsx`'s direct-to-Supabase client writes (`internal_crypto_rates`, `platform_rates`, previously done with the anon key) into the `crypto-rates` POST route server-side.
  - Disabled public self-signup (`app/signup/page.tsx` now redirects to `/login`; removed the "Create account" link).
  - **Wrote but did NOT apply** `supabase/migrations/fix_rls_policies.sql` — drops the `WITH CHECK (auth.role() = 'service_role' OR true)` holes on `external_rate_sources`/`fx_rate_calculations`/`internal_crypto_rates`/`platform_rates`, and enables RLS (previously fully open, no RLS at all) on `otc_desk_rates`/`rate_calculation_logs`. This needs Sunny to review and run it against production — per standing policy, migrations against a live DB are never run without his explicit go-ahead.
- **Bug fixes**: deprecated `usd-ngn-engine` (wrong Binance symbol, hardcoded-to-fail CMC, silently served stale `1550`) → `410`; pruned `aggregator`'s vendor list to routes that actually exist (7 referenced sub-routes never existed, 404ing silently every call); added finite/positive validation to the `vertofx`/`aza` scrapers; extracted vendor-fetch logic out of `app/api/fx/vendors/route.ts` into `lib/api/fetchVendorRates.ts` and pointed `platform-rates`/`update-platform-rates` at it directly instead of self-referential HTTP fetches; replaced the in-memory (and accidentally-global, not per-caller) rate limiter with a durable Supabase-backed one (`lib/fx-engine/utils/rate-limiter.ts` + new **unapplied** migration `add_rate_limit_events_table.sql`); added auth/rate-limiting to the previously-open `rate-fallback`/`rate-proxy` routes; removed dead commented code and debug `console.log`s from `vendors/route.ts`; untracked `test-vendors.js`/`build_output.log` from git (added to `.gitignore`).
- **Verification**: `yarn build` passes clean (Next.js 16.1.0-canary.19, Turbopack) after fixing one TS strict-typing error in the new cookie-handling code. `yarn lint` / `next lint` is broken independent of these changes — the subcommand was removed in this Next canary, and direct ESLint 9 also fails on a pre-existing flat-config/circular-reference issue in this repo's setup. No manual QA yet: login flow, the OTC/crypto admin forms end-to-end, and desktop/mobile screenshots of the new palette are all still outstanding.
- **Flagged, not touched** (bigger judgment calls, listed for a separate go/no-go): Next.js on a pre-release canary version; redundant `chart.js` + `recharts`; zero test suite/CI; unimplemented admin-alert TODO in `lib/fx-engine/utils/fallback.ts`; `middleware.ts` naming is deprecated in this Next canary in favor of `proxy.ts` (still functional, just warns on build).
- **Not started**: Workstream 4 (public partner rate API) — waiting on Sunny's Vercel/production check from Workstream 0.
